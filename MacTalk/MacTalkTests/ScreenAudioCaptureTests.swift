// Legacy ScreenCaptureKit smoke tests were removed after review because they
// either tested Apple APIs, depended on host TCC state, or never started capture.
// Callback synchronization regressions remain covered in ConcurrencyStressTests.
